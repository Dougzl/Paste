<ui:FluentWindow
    x:Class="Paste.App.Views.Windows.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Settings"
    Width="400"
    Height="280"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True">

    <Grid Margin="16,40,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Hotkey -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Global Hotkey" FontWeight="SemiBold" FontSize="14"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6" />
            <TextBox x:Name="HotkeyBox"
                     IsReadOnly="True"
                     FontSize="13"
                     PreviewKeyDown="HotkeyBox_PreviewKeyDown"
                     GotFocus="HotkeyBox_GotFocus"
                     LostFocus="HotkeyBox_LostFocus" />
        </StackPanel>

        <!-- Auto Cleanup -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Auto-Cleanup" FontWeight="SemiBold" FontSize="14"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6" />
            <ComboBox x:Name="CleanupCombo" FontSize="13" SelectedIndex="0">
                <ComboBoxItem Content="Never" Tag="0" />
                <ComboBoxItem Content="1 day" Tag="1" />
                <ComboBoxItem Content="3 days" Tag="3" />
                <ComboBoxItem Content="7 days" Tag="7" />
                <ComboBoxItem Content="14 days" Tag="14" />
                <ComboBoxItem Content="30 days" Tag="30" />
                <ComboBoxItem Content="90 days" Tag="90" />
            </ComboBox>
        </StackPanel>

        <!-- Save / Cancel -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click" />
            <ui:Button Content="Save" Appearance="Primary" Padding="16,6" Click="Save_Click" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
